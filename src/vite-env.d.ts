/// <reference types="vite/client" />

// Injected by vite.config.ts / vitest.config.ts from src-tauri/tauri.conf.json
// (the canonical version source; see tests/version.test.ts).
declare const __APP_VERSION__: string;
